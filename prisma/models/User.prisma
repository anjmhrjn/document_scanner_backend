model User {
  id            Int      @id @default(autoincrement())
  email         String
  username      String   @unique
  firstname     String?
  lastname      String?
  hash          String
  salt          String
}